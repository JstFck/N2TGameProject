class Raycaster {
    field Map map;

    constructor Raycaster new(Map initMap) {
        let map = initMap;
        return this;
    }

    method void dispose() {
        do Memory.deAlloc(this);
        return;
    }
}